# Release Notes — 2026-03-22

## Title
Coretto reaches `v1.3c` baseline with majority-vote gate pass

## Highlights
- Established `v1.3c` as current working baseline
- Built baseline engineering pack with gate / observe / stress structure
- Fixed evaluation-chain issues that previously polluted gate reports
- Introduced 3-run majority-vote gate strategy
- Confirmed gate pass on current baseline

## Why it matters
Coretto is no longer only a prompt iteration project.
It now has a reproducible engineering baseline, which makes future iteration comparable and auditable.

## Recommended next step
Freeze this baseline and perform future prompt work only via new versions that compare against it.
